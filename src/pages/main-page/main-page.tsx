import { Header, Hero, GetSection, PostSection } from "../../widgets";

export const MainPage = () => {
    return (
        <>
            <Header />
            <Hero />
            <GetSection />
            <PostSection />
        </>
    );
}